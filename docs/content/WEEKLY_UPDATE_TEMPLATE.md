# memware 周更新模板

> 没有用户可感知的新信息时不发布。删除所有占位符和未经验证的表达。

## 本周一个变化

`<用户现在能完成什么，或哪个明确失败得到修复>`

## 为什么重要

`<对应哪类用户、此前有什么阻碍>`

## 证据

- Commit / PR：`<链接>`
- 实际验证命令：`<命令>`
- 结果与验证层：`<source / test / build / package / runtime>`
- 支持范围：`<平台、版本、模型接口>`

## 边界

`<没有验证什么、仍不可用什么、数据会发送到哪里>`

## 唯一 CTA

`<复现 / 阅读 / 提交反馈，三选一>`
